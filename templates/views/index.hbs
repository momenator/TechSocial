{{!< default}}
<div class="container">
	<div class="col-md-8">
		<h1>Welcome to the techSocial Network
		{{#if user}}
			<h1>Hello {{user.name.first}} {{user.name.last}}</h1>
		{{else}}
		<p>
			<a href="/signin">Sign in</a>
		</p>
		<p>
			<a href="/signup">Sign up</a>
		</p>
		{{/if}}
	</div>
</div>
